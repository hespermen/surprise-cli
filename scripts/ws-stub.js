// Заглушка вместо ws.
//
// В бандл приезжал полный стек WebSocket — четырнадцать модулей, около двух
// тысяч строк: разбор кадров, распаковка deflate, валидация UTF-8. Нужен он
// был одному react-devtools-core, который здесь уже заглушён строкой выше в
// build.mjs, так что вызвать этот код было некому.
//
// Дело не только в десятой части веса мегабайтного артефакта, который и так
// невозможно отревьюить глазами. Парсер бинарного протокола — классический
// источник уязвимостей, и держать его в плеере «на всякий случай» значит
// расширять поверхность атаки ради кода, который никогда не исполняется.
//
// Если ws однажды понадобится по-настоящему, заглушку надо убрать осознанно —
// и тогда же ответить, зачем плееру разбирать чужие кадры.
export default class WebSocketStub {
  constructor() {
    throw new Error("ws в surprise-cli не используется (см. scripts/ws-stub.js)");
  }
}
